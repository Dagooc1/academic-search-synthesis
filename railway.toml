[build]
builder = "nixpacks"
builderSettings = { nixpacks = { buildCommand = "pip install -r requirements.txt" } }

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT"
healthCheckPath = "/"
healthCheckTimeout = 60

[variables]
PORT = "5000"